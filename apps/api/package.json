{
  "name": "@ringbacksms/api",
  "version": "0.0.1",
  "private": true,
  "description": "Prisma schema, migrations, and seed scripts. The Express server that used to live here (a duplicate of apps/web's logic, targeted at a deployment that was never used) was decommissioned 2026-07-28 — apps/web is the single production stack and generates its Prisma client from this package's schema.",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate deploy",
    "db:seed": "ts-node prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "prisma": "^5.8.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
